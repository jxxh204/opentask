node_modules
dist
.DS_Store
*.log
server/test-runs
.playwright-mcp
tsconfig.tsbuildinfo
.env
.env.local

# openRM local data (SQLite DB + secrets) — holds per-user state, never commit
.openrm/
# flat-JSON runtime state written by server/*.cjs (settings/tasks/sessions/…) — holds tokens/state, never commit
.openrm-*
