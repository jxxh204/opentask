# OpenTask 개발 기록

Claude Code와 페어로 OpenTask(구 OpenRM)를 만들며 오간 대화·결정·버그를 나중에 블로그로 옮길 수 있게 정리한 기록. 시간순이 아니라 주제별로 묶었다.

## 1. 크론잡(Automations) 기능 신설

- **계기**: Electron 창을 열어 실제로 화면을 확인하던 중 "크론잡 만드는 페이지는?"이라는 질문에서 시작. 기존엔 없던 기능이었다.
- **결정**: OS 레벨 cron/launchd 연동은 하지 않기로 함 — "로컬 전용 도구에 과한 인프라"라는 판단. `monitor.cjs`/`notify.cjs`와 같은 패턴으로 30초 폴링(`setInterval`) 방식의 자체 스케줄러(`server/scheduler.cjs`)를 채택. 앱이 켜져 있을 때만 동작한다는 제약을 그대로 받아들임.
- **스케줄 종류**: interval(N분마다) / daily(매일 HH:MM) / weekly(요일+시각) 세 가지. 액션은 지금은 "새 일감 생성" 하나뿐 — "AI가 스스로 작업 범위를 만들지 않는다"는 기존 원칙과 충돌하지 않도록, 사람이 미리 정한 스케줄을 그대로 실행하는 것으로 한정.
- **DB**: `cron_jobs` 테이블 신설(SQLite migration v8). `computeNextRun()`으로 세 스케줄 타입의 다음 실행 시각을 계산하는 로직을 격리해서 테스트.

## 2. "이미지와 완전 동일한 UI로 해줘" — 레퍼런스 기반 사이드바 리디자인

- Orca 앱 사이드바 스크린샷을 레퍼런스로 받아 비교. 처음엔 "어느 정도 비슷하게"였다가, 다음 라운드에 "완전 동일하게"로 요구 수준이 올라감.
- 구조(레포 드롭다운 vs 플랫 리스트)까지 통째로 바꿀지, 비주얼 스타일만 정밀 매칭할지 먼저 물어봄 → **비주얼 스타일만**으로 결정. 구조를 바꾸는 건 범위가 훨씬 크다는 이유.
- 결과물: 검색창(실제로 일감/태스크 트리를 필터링하는 기능 포함, 장식용 아님), "Projects" 헤더 스타일의 레포 라벨 + 고스트 아이콘 버튼 클러스터(레포 추가 폴더 아이콘 + 태스크 추가), nav 링크 타이포그래피를 보관함/설정 행과 통일.
- **교훈**: 텍스트로 "이 정도 색이 전문적이에요"라고 설명하는 것보다, 실제 사이드바 요소(선택된 메뉴/버튼/배지)에 후보 색을 입혀 나란히 비교할 수 있는 미니 목업 아티팩트를 만드는 게 결정 속도를 훨씬 높였다.

## 3. "레거시 화면 같은데" — 듀얼레일 Shell과의 결별

- "크론잡" 메뉴를 처음 라우팅으로 연결했더니, 세션 화면(라이트 테마, 새 사이드바)과 전혀 다른 어두운 화면이 떴다. 알고 보니 `/automations`가 예전 VSCode 스타일 듀얼레일 Shell(ActivityBar+ContextPanel, 항상 다크) 경로에 물려 있었던 것.
- 사용자 반응: "내가 만들던 화면과 전혀 다르잖아" → "레거시 화면 같은데" → "크론잡이 탭에 추가되면 돼. 규칙이야, 모든 메뉴는 탭에서 나온다."
- **새 규칙 확정**: 모든 메뉴는 SessionShell의 탭 워크스페이스 안에서 열려야 한다. 별도 라우트로 이동하지 않는다.
- **구현**: 태스크/워크트리에 속하지 않는 전역 메뉴(크론잡, 모델 배정)를 위해 `CRONJOBS_NODE_ID`/`MODEL_POLICY_NODE_ID` 같은 고정 가짜 노드 id를 만들어, 기존 탭 인프라(`tabsByNode` 등)를 그대로 재사용. 실제 트리 노드가 아니어도 탭바·탭 인스턴스 개념을 그대로 쓸 수 있었다.
- **스코프 결정**: Debug/GitHub/Monitor/Architecture/Setup까지 전부 한 번에 옮기면 범위가 너무 커서, 이번엔 크론잡만 탭화하고 나머지는 다음 라운드로 미룸. 다만 이 5개는 현재 SessionShell 어디서도 링크가 없어 실사용 중에는 마주칠 일이 없다는 점도 확인.

## 4. 설정 모달이 찌그러지던 버그

- "설정 깨진다"는 짧은 리포트로 시작. 스크린샷을 보니 "내부 용어 언어" 라벨이 "내브 요어 어어"처럼 글자 아래쪽이 잘려 보이는 기이한 현상.
- **원인 추적**: 모델 배정 섹션이 세션 내내 계속 늘어나(16개 작업 종류) 모달 전체 높이가 `max-height: 86vh`를 넘어섰다. `.body`(테마/언어 토글을 담은 컨테이너)만 `min-height: 0`이 걸려 있어서, flexbox가 부족한 공간을 오직 이 요소에서만 쥐어짜냈고, 그 결과 두 번째 줄(언어 토글)이 컨테이너 자체의 `overflow-y: auto` 경계에 잘려 렌더링됐다. 한글 받침이 잘려 다른 글자처럼 보인 것.
- **고친 방법**: 단순 CSS 패치 대신, 마침 진행 중이던 "모든 메뉴는 탭에서" 규칙에 맞춰 모델 배정 섹션을 통째로 별도 탭(`ModelPolicyPane`)으로 분리. 모달은 테마/언어 토글만 남아 절대 넘칠 일이 없는 크기가 됐고, 모델 배정은 탭이라 높이 제한 자체가 없어졌다 — 버그 수정과 아키텍처 정리가 한 번에 해결된 케이스.

## 5. 브랜드 컬러 교체 — 바이올렛 → 그레이스케일

- "메인 컬러가 보라색인데 좀 더 전문성 있는 CLI 툴 컬러로 해줘"라는 요청. 텍스트만으로 색을 설명하지 않고, 실제 사이드바 요소(선택 상태·버튼·배지)에 4개 후보(슬레이트 블루/티얼/그레이스케일/앰버)를 입힌 비교 아티팩트를 만들어 보여줌.
- **선택**: 그레이스케일(#5B6472, 다크 테마 #9BA4B3). 액센트를 거의 죽이고 상태색(초록/파랑/빨강)만으로 의미를 구분하는 가장 절제된 방향.
- 토큰 하나(`--violet`/`--vtint`)만 바꾸면 버튼·배지·선택 상태 전체에 자동 반영되는 구조 덕분에 앱 전체 리브랜딩이 CSS 커스텀 프로퍼티 3곳 교체로 끝남. 단, light/dark 두 블록 다 손봐야 했고, `replace_all`이 들여쓰기 차이 때문에 한쪽만 바꾸고 넘어가는 실수가 있었음 — 반드시 `grep`으로 남은 값이 없는지 재확인하는 습관이 필요했다.

## 6. "+ 버튼 하나로 뭘 하는지 안 보인다" — 태스크 추가 버튼 분리

- 아이콘 전용 고스트 버튼 클러스터(레포 추가/일감 생성)로 통일했다가, "일감추가 버튼을 따로 만들어줘, +버튼으로 하지 말고. 태스크 추가로 이름을"이라는 피드백.
- **버그**: 클래스 이름만 `taskAddBtn`으로 바꾸고 정작 CSS를 안 만들어서, 브라우저가 아이콘 전용 SVG를 기본 크기(뷰박스만 있고 width/height 없는 SVG는 300×150px로 렌더링됨)로 그려버려 사이드바에 거대한 회색 박스가 뜨는 사고가 남. 사용자가 스크린샷으로 캡처해서 알려줌 — "CSS 클래스를 JSX에서 바꿨으면 그 클래스가 실제로 정의돼 있는지"는 매번 확인이 필요한 지점.

## 7. base 브랜치가 항상 'main'으로 강제되던 버그

- 새 일감을 태스크로 등록하는 확인 화면에서 base 브랜치 기본값이 하드코딩된 `'main'`이었다.
- **왜 문제였나**: 백엔드(`worktrees.cjs`)는 이미 똑똑한 폴백 체인을 갖고 있었다 — 명시적 base → 레포 설정 base → 레포의 현재 체크아웃 브랜치 자동감지 → 최후 수단 `'main'`. 그런데 프론트가 매번 `'main'`을 명시적으로 채워 보내니, 이 체인이 아예 실행되지 않고 항상 `'main'`이 이겼다. main이 아닌 브랜치(master/develop 등)를 쓰는 레포에서는 매번 틀린 base로 워크트리가 만들어질 뻔한 셈.
- **수정**: 기본값을 빈 문자열로. 안 건드리면 빈 값이 전송되고, `folders.cjs`의 `base || null` 정규화를 거쳐 백엔드의 자동감지가 실제로 작동하게 됨. 요약 줄엔 "자동감지"라는 라벨로 안내.
- **후속 논의**: 폴더 안에 여러 태스크(워크트리)가 있을 때, 첫 태스크 생성 시점에 감지된 base를 폴더에 고정해서 이후 태스크들이 서로 다른 시점의 "현재 체크아웃 브랜치"로 갈라지지 않게 하는 개선도 함께 적용(`orchestrator.cjs`).

## 8. 진짜 레포 구조에서 발견한 워크트리 문제 (진행 중)

- "gongbiz 폴더를 예시로 봐줘. 안에 워크트리와 메인 트리도 함께 있어"라는 지적으로 시작. 실제로 확인해보니 OpenTask에 등록된 "gongbiz" 레포는 `.git`이 없는 **상위 폴더**였고, 진짜 레포는 그 안의 `gongbiz-crm-b2b-web`이었다. 그 진짜 레포의 워크트리 20개가 전부 부모 폴더에 형제 디렉터리로 흩어져 있었던 것.
- `git worktree list --porcelain`으로 직접 확인 — 이미 서버 코드(`worktrees.cjs`의 `list()`)에 거의 완성된 파싱 로직이 있었지만, 멀티레포 이전 시절 코드라 단일 레포(`C.REPO`) 전용이었고 프론트 어디서도 안 쓰이는 죽은 API(`/api/worktrees`)였다는 것도 함께 발견.
- **합의된 설계**(구현 착수): 레포 피커 드롭다운 안에 "이 레포의 워크트리" 섹션을 추가. 추적됨(OpenTask 태스크로 관리 중)이면 태스크명 표시 + 클릭 시 그 탭으로 이동 + "연결 해제" 아이콘(추적만 제거, 실제 git worktree/브랜치는 안 건드림). 미추적(다른 도구·다른 사람이 만든 워크트리)이면 브랜치명 표시 + 클릭 시 그 경로로 터미널 탭 바로 열기(추적 안 됨) + "연결" 아이콘(새 워크트리를 만들지 않고 기존 경로에 Task/Branch 레코드만 생성해 입양).

## 9. "완료 체크는 검증이 아니라 자가신고였다" — 상태 판정 로직 전수 감사

- **계기**: "레포 생성" 서브태스크가 GitHub 리네임·푸시를 빼먹은 채로 완료(done) 체크가 찍힌 사건. 원인을 추적하니 `advance_subtask_work`는 세션 자신이 "다 했다"고 호출하는 자가신고일 뿐, 서버는 커밋·푸시·테스트 등 무엇 하나 검증하지 않고 즉시 done을 찍고 다음 단계를 시작하고 있었다.
- **감사 범위**: 이번 기회에 앱 전체에서 "상태"를 판정하는 로직 7가지를 신뢰도별로 감사 — 자가신고(무검증) / 휴리스틱(추정) / 외부 상태 실제 조회, 세 등급으로 분류.
- **결과**: `done`(완료 체크), `stalled/amber`(무응답), `repo_auto`(레포 자동배정) 3개가 자가신고·무검증 등급. `alive`(세션 생존)·`blocked`(도움요청)·코드 verdict는 휴리스틱 등급. PR/CI 상태만 GitHub API를 실제로 조회하는 유일한 "높음" 등급 — 이 앱에서 가장 신뢰할 수 있는 참조 모델.
- **즉시 조치**: `repo_auto`는 재확인을 강제하는 장치가 전혀 없어 코드 구조 단서만으로 오배정된 전례가 있었다(§ "확인 없이 재배정하지 말 것" 원칙과 정확히 충돌). 감사 직후 자동배정 호출 자체를 제거(`index.cjs`, `useSessionsStore.ts`) — 이제 레포는 항상 사람이 직접 고른다. `repoClassify.cjs` 모듈과 DB 컬럼은 되돌리기 쉽게 남겨둠.
- **남은 갭(제안 단계)**: `done`에 완료 요약+잔여 항목 제출을 요구하는 게이트, `stalled`의 PTY 텍스트 의존도를 낮추는 것, advance가 지휘자 확인 전까지 다음 단계를 켜지 않는 옵션 게이트.
- **곁가지 교훈**: 다이어그램을 그리며 세션이 "tmux 세션"에서 뜬다고 썼다가 스스로 걸렸다 — `term.cjs`를 열어보니 tmux는 "다른 사람이 쓸 때 불편해서" 이미 오래전에 제거됐고, 지금은 node-pty(번들된 네이티브 모듈, 외부 설치 불필요) + 헤드리스 xterm으로 대체돼 있었다. 코드에 `tmuxSession`/`Term.list()` 같은 옛 이름만 남아있어 확인 없이 넘겨짚은 것 — 이 감사 자체가 지적한 "검증 없이 판단하기"를 그대로 반복한 셈이라 좋은 반면교사가 됐다.
- **다이어그램**([`docs/status-flow.svg`](docs/status-flow.svg))도 처음엔 라벨이 화살표 선에 겹치거나 잘리는 렌더링 버그가 있었는데, 좌표만 땜질하지 않고 세 분기가 서로 다른 높이에서만 갈라지는 버스(bus) 구조로 연결 방식 자체를 바꿔 재발 여지를 없앴다. 헤드리스 크롬으로 직접 스크린샷을 떠서 확인한 뒤에야 반영 — 여기서도 "만들어놓고 검증 안 하기"는 피했다.

  ![서브태스크 상태 판정 흐름 — advance 루프는 검증 화살표 없이 곧장 반복되고, PR/CI만 GitHub API를 실제로 조회한다](docs/status-flow.svg)

---

*이 문서는 대화 중 요청으로 생성됐고, 새 라운드가 끝날 때마다 이어서 갱신하면 됩니다.*
